package fingerprint

// Canonical state ledgers (v5). Landing in the next build: per-entity
// histories for location, possession, condition/life status, knowledge,
// goals, obligations, and relationships, folded from typed frames with
// conflicting values preserved.

import "draftline/internal/types"

func buildLedgers(frames []types.NarrativeFrame) []types.StateLedger {
	return nil
}
